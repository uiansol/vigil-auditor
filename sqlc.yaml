version: "2"
sql:
  - schema: "deployments/db/schema.sql"
    queries: "deployments/db/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "pkg/db"
        sql_package: "pgx/v5" # Uses modern, high-performance pgx pool
