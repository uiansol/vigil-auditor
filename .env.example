# Postgres (Compose service hostname is "postgres" inside the network)
DATABASE_URL=postgres://vigil:vigil@localhost:5432/vigil?sslmode=disable

# Go gateway
GATEWAY_ADDR=:8080
CORS_ORIGINS=http://localhost:3000
MAX_CONCURRENT_AUDITS=4
SSE_STAGE_DELAY_MS=400

# Python AI service
AI_HTTP_ADDR=0.0.0.0:8081
AI_GRPC_ADDR=0.0.0.0:50051

# Next.js dashboard (browser calls host-mapped gateway)
NEXT_PUBLIC_API_URL=http://localhost:8080
